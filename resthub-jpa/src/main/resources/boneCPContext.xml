<beans profile="resthub-pool-bonecp" xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:util="http://www.springframework.org/schema/util"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/util
        http://www.springframework.org/schema/util/spring-util.xsd">

    <bean id="boneCPDataSourceFactory" class="org.resthub.jpa.pool.BoneCPDataSourceFactory"/>

    <bean id="dataSource" class="com.jolbox.bonecp.BoneCPDataSource" destroy-method="close"
          factory-bean="boneCPDataSourceFactory" factory-method="create">
          <constructor-arg ref="dataSourceProperties"/>
    </bean>

    <!-- we must map all configuration props to allow overriding any prop in database.properties-->
    <util:properties id="dataSourceProperties">
        <prop key="driverClass">${dataSource.driverClassName}</prop>
        <prop key="jdbcUrl">${dataSource.url}</prop>
        <prop key="username">${dataSource.username}</prop>
        <prop key="password">${dataSource.password}</prop>

        <prop key="maxConnectionsPerPartition">${dataSource.maxConnectionsPerPartition}</prop>
        <prop key="minConnectionsPerPartition">${dataSource.minConnectionsPerPartition}</prop>
        <prop key="partitionCount">${dataSource.partitionCount}</prop>
        <prop key="connectionTestStatement">${dataSource.connectionTestStatement}</prop>
        <prop key="idleConnectionTestPeriodInMinutes">${dataSource.idleConnectionTestPeriodInMinutes}</prop>
        <prop key="statementsCacheSize">${dataSource.statementsCacheSize}</prop>
        <prop key="poolName">${dataSource.poolName}</prop>

    </util:properties>

</beans>
